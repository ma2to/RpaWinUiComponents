<!-- FINÁLNY UNIFIKOVANÝ XAML - Opravené všetky XAML chyby -->
<!-- SÚBOR: RpaWinUiComponents/AdvancedWinUiDataGrid/Views/UnifiedAdvancedDataGridControl.xaml -->

<UserControl
    x:Class="RpaWinUiComponents.AdvancedWinUiDataGrid.Views.UnifiedAdvancedDataGridControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="White">

    <UserControl.Resources>
        <!-- Štýly pre komponenty -->
        <x:Double x:Key="CellMinHeight">35</x:Double>

        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,10"/>
        </Style>

        <Style x:Key="StatusTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Background" Value="#17A2B8"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Status Bar -->
            <RowDefinition Height="*"/>
            <!-- Main Content -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Status Bar -->
        <Border Grid.Row="0" 
                Background="#F8F9FA" 
                BorderBrush="#DEE2E6" 
                BorderThickness="0,0,0,1" 
                Padding="12">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📊 RpaWinUiComponents DataGrid" 
                           FontSize="14" 
                           FontWeight="Bold" 
                           Foreground="#1976D2" 
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="StatusIndicator" 
                           Text=" - Loading..." 
                           FontSize="12" 
                           Foreground="#666" 
                           VerticalAlignment="Center" 
                           Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" x:Name="MainContentGrid">
            <!-- Loading Panel -->
            <StackPanel x:Name="LoadingPanel" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Visibility="Visible">
                <ProgressRing IsActive="True" Width="50" Height="50"/>
                <TextBlock Text="Načítava sa DataGrid..." 
                          FontSize="14" 
                          Foreground="#666"
                          HorizontalAlignment="Center"
                          Margin="0,16,0,0"/>
            </StackPanel>

            <!-- DataGrid Container s ScrollViewer -->
            <ScrollViewer x:Name="MainScrollViewer"
                          Visibility="Collapsed"
                          ZoomMode="Disabled"
                          HorizontalScrollMode="Auto"
                          VerticalScrollMode="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          Background="White">

                <Grid x:Name="DataGridContainer" Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Header -->
                        <RowDefinition Height="*"/>
                        <!-- Data -->
                    </Grid.RowDefinitions>

                    <!-- Header Row -->
                    <Border Grid.Row="0" 
                            Background="#F8F9FA" 
                            BorderBrush="#DEE2E6" 
                            BorderThickness="1"
                            CornerRadius="4,4,0,0">
                        <ScrollViewer x:Name="HeaderScrollViewer"
                                      HorizontalScrollMode="Auto"
                                      VerticalScrollMode="Disabled"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Disabled"
                                      ZoomMode="Disabled">
                            <StackPanel x:Name="HeaderPanel" 
                                       Orientation="Horizontal" 
                                       Background="#F8F9FA"
                                       MinHeight="45"/>
                        </ScrollViewer>
                    </Border>

                    <!-- Data Rows -->
                    <ScrollViewer Grid.Row="1"
                                  x:Name="DataScrollViewer"
                                  HorizontalScrollMode="Auto"
                                  VerticalScrollMode="Auto"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  ZoomMode="Disabled">
                        <StackPanel x:Name="DataRowsPanel" 
                                   Background="White"/>
                    </ScrollViewer>
                </Grid>

            </ScrollViewer>

            <!-- Empty State -->
            <StackPanel x:Name="EmptyStatePanel"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="Collapsed">
                <TextBlock Text="📄 Žiadne dáta" 
                          FontSize="16" 
                          FontWeight="Bold" 
                          Foreground="#999"
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Použite LoadDataAsync() na načítanie dát" 
                          FontSize="12" 
                          Foreground="#BBB"
                          HorizontalAlignment="Center"
                          Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Status Footer -->
        <Border Grid.Row="2" 
                Background="#F5F5F5" 
                BorderBrush="#E0E0E0" 
                BorderThickness="0,1,0,0" 
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="16">
                    <TextBlock x:Name="StatusText" 
                              Text="Pripravuje sa..." 
                              Style="{StaticResource StatusTextStyle}"/>
                    <TextBlock x:Name="RowCountText" 
                              Text="0 riadkov" 
                              FontSize="11" 
                              Foreground="#999" 
                              VerticalAlignment="Center"/>
                    <TextBlock x:Name="ValidationText" 
                              Text="" 
                              FontSize="11" 
                              Foreground="Red" 
                              VerticalAlignment="Center"/>
                    <TextBlock x:Name="MemoryText"
                              Text=""
                              FontSize="11"
                              Foreground="#999"
                              VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="F2: Edit | Enter: Next | Tab: Navigate | Ctrl+C/V: Copy/Paste" 
                              FontSize="10" 
                              Foreground="#999" 
                              VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</UserControl>